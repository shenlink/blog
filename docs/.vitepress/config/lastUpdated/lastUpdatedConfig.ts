import { LastUpdatedConfig } from "../category/types";

export const lastUpdated: LastUpdatedConfig = {
    text: '最后更新',
    formatOptions: {
        dateStyle: 'medium',
        timeStyle: 'medium'
    }
};
