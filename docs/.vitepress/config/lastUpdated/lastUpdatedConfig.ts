import { DefaultTheme } from "vitepress";

export const lastUpdated: DefaultTheme.LastUpdatedOptions = {
    text: '最后更新',
    formatOptions: {
        dateStyle: 'medium',
        timeStyle: 'medium'
    }
};
