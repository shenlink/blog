{
  "devDependencies": {
    "@types/node": "^22.9.0",
    "chokidar": "^4.0.1",
    "gray-matter": "^4.0.3",
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "push:both": "git push origin master && git push github master"
  }
}